package org.coteis.repository;

import org.coteis.domain.Previoustest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrevioustestRepository extends JpaRepository<Previoustest, Integer> {
}
